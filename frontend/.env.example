# Optional: public URL of the Express API (Fly.io), no trailing slash.
# Vite loads env from the repo root `.env` — add this line there, or put it in frontend/.env.local.
# Leave unset for local dev when the backend runs on localhost:3001 (Vite proxies /api).
# VITE_API_URL=https://your-app.fly.dev
